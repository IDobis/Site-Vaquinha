/** Configuração pública (carregada antes dos outros scripts). */
window.siteConfig = {
  /** Chave PIX (CPF). É o texto copiado pelo botão “Copiar chave PIX”. */
  pixKey: "14073635905",

  /** Meta da campanha em reais (número). */
  metaReais: 2000,
  /** Quanto já foi arrecadado em reais (atualize quando receber doações). */
  arrecadadoReais: 254.01,

  /**
   * Nomes dos doadores (ordem livre). Ex.: "Ana", "João — R$ 50"
   * Deixe `[]` até ir preenchendo.
   */
  doadores: ["Ditzel", "Gustavo Bayer", "Daniele Tomazoni", "Pedro Tomazoni"],
};
